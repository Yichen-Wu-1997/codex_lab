1. define SKILL.md and AGENT.md
2. plan first (result in a markdown file; xhigh) -> have another plan mode to review the plan (the markdown file; xhigh) -> implement the code (not plan mode; high) -> review the implementation using plan mode again (xhigh)
3. let agent ask for permissions before implementations
4. different instances should access different files/scopes (and different models for different tasks)
5. use '/compact' when a thread has gone too long
x. inspect the templates for insights



## my questions
- what should be included in agent.md
    - check agent docs
- what should be included in skills.md
    - where to put it? .agents\skills\explain-change? what is this place? 
    - check skill docs
- what a config.toml does? when there are multiple ones, what is the priorities?
    - where to put it? \.codex?
- differences between CHAT and CODEX in vscode IDE
- what does configure agent do?
- should I open different subfolder to have different instances. or should I just create different chat threads?

