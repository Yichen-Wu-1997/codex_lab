# Task

Ask Codex this:

```text
Read the source and current tests first.
Then add tests for a new method named search_titles(keyword) on ReadingList.
The search should be case-insensitive, preserve the original order, and return matching titles.
Only after the tests are in place should you implement the feature and run the test suite.
Keep the change small.
```
